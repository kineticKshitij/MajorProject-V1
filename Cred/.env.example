# ==============================================================================
# AI-Enhanced Google Dorks Toolkit - Environment Configuration
# ==============================================================================
# Copy this file to Cred/.env and fill in your actual credentials
# NEVER commit the .env file to Git - it contains sensitive information!
# ==============================================================================

# ==============================================================================
# DJANGO SETTINGS
# ==============================================================================
DEBUG=True  # Set to False in production
DJANGO_SECRET_KEY=your-secret-key-here-change-this-in-production

# ==============================================================================
# DATABASE CONFIGURATION
# ==============================================================================
# SQLite (Default - for development)
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3

# PostgreSQL (Uncomment for production)
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=googledorks_db
# DB_USER=postgres_user
# DB_PASSWORD=postgres_password
# DB_HOST=localhost
# DB_PORT=5432

# ==============================================================================
# EMAIL CONFIGURATION (Gmail SMTP)
# ==============================================================================
# Required for sending alert notifications and automated emails
# To get Gmail App Password:
# 1. Enable 2-Step Verification on your Google Account
# 2. Go to: https://myaccount.google.com/apppasswords
# 3. Generate app password for "Mail" on "Windows Computer"
# 4. Copy the 16-character password below

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your.email@gmail.com
EMAIL_HOST_PASSWORD=your-16-character-app-password
DEFAULT_FROM_EMAIL=your.email@gmail.com

# ==============================================================================
# API KEYS - GOOGLE GEMINI AI
# ==============================================================================
# Required for AI-powered chatbot and entity research features
# Get your API key from: https://makersuite.google.com/app/apikey

GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.0-flash-exp

# ==============================================================================
# API KEYS - RAPIDAPI (BreachDirectory)
# ==============================================================================
# Required for breach data lookup and security research
# Get your API key from: https://rapidapi.com/rohan-patra/api/breachdirectory

RAPIDAPI_KEY=your-rapidapi-key-here
USE_REAL_BREACH_API=True  # Set to False to use demo mode

# ==============================================================================
# CORS CONFIGURATION (Frontend URLs)
# ==============================================================================
# Add your frontend development server URLs here
CORS_ORIGIN_1=http://localhost:3000
CORS_ORIGIN_2=http://localhost:5173
CORS_ORIGIN_3=http://127.0.0.1:3000
CORS_ORIGIN_4=http://127.0.0.1:5173

# ==============================================================================
# TOR PROXY CONFIGURATION (Anonymous Web Scraping)
# ==============================================================================
# Enable Tor integration for anonymous and IP-rotating web scraping
# Requires Docker Tor container: docker-compose -f docker-compose.tor.yml up -d
# Documentation: docs/TOR_INTEGRATION_ARCHITECTURE.md

# Enable/disable Tor proxy
TOR_ENABLED=false  # Set to 'true' to route crawler requests through Tor

# Tor proxy connection settings
TOR_PROXY_HOST=localhost  # Use 'tor-proxy' if running Django in Docker
TOR_PROXY_PORT=9050       # SOCKS5 proxy port

# Tor control port settings (for circuit rotation)
TOR_CONTROL_PORT=9051     # Control port for managing circuits
TOR_CONTROL_PASSWORD=your-tor-control-password  # Change in production!

# Circuit rotation settings
TOR_CIRCUIT_TIMEOUT=60    # How often to rotate circuit (seconds)
TOR_MAX_RETRIES=3         # Max retries on failed requests
TOR_AUTO_ROTATE_AFTER=0   # Auto-rotate after N requests (0 = disabled)

# ==============================================================================
# PRODUCTION SETTINGS (Uncomment and configure for production deployment)
# ==============================================================================
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
# SECURE_HSTS_SECONDS=31536000
# SECURE_HSTS_INCLUDE_SUBDOMAINS=True
# SECURE_HSTS_PRELOAD=True

# ==============================================================================
# NOTES
# ==============================================================================
# - For local development, you can use DEBUG=True and console email backend
# - For production, always set DEBUG=False and configure real SMTP
# - Generate a strong SECRET_KEY using: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
# - Keep this file secure and never share it publicly
# - Each team member should have their own .env file with personal credentials
# ==============================================================================
